<Window x:Class="WinIslands.UI.CoverFullScreenWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        ShowActivated="False" WindowStartupLocation="Manual"
        PreviewKeyDown="OnPreviewKeyDown" MouseRightButtonUp="Root_MouseRightButtonUp" Cursor="Hand">
    <Grid x:Name="Root" MouseLeftButtonUp="Root_MouseLeftButtonUp" Background="#E9000000">
        <Grid>
            <Image x:Name="Art" Stretch="Uniform" Margin="56"
                   RenderOptions.BitmapScalingMode="HighQuality"
                   VerticalAlignment="Center" HorizontalAlignment="Center"/>
            <TextBlock Text="点击任意处关闭  ·  Esc" FontSize="11.5" Foreground="#99FFFFFF"
                       HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,28"/>
        </Grid>
    </Grid>
</Window>
